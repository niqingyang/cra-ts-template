import {defineMessages} from "react-intl";

const messages = defineMessages({
    title: 'Clicked: {count} times, render {num} times ',
    nameTitle: 'Name: {name}, render times {num} '
});

export default messages;
